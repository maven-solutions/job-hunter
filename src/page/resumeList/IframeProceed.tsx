import React from "react";

const IframeProceed = () => {
  return (
    <div className="ci_iframe_gradient_box">
      Open the job application in a new tab to continue the auto-fill process.
      Click <strong>'Proceed'</strong> to open the job in a new tab.
    </div>
  );
};

export default IframeProceed;
